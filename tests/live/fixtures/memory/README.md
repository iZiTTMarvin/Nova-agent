# 临时注入实验对照

此目录冻结了迁出前的 `MemoryPrefetchService`、`MemoryPrefetchWiring` 及相关预算常量，供 `memoryUtility.spec.ts` 比较临时注入与普通工具历史的请求前缀行为。生产代码不得依赖此目录。

对照仅保留原注入位置与生命周期；使用当前检索器，执行状态输入为空。因此不能把它称为原版本完整效果复测，也不能用其通过结果证明生产自动召回仍然存在。

该策略不随产品功能演进。真实模型缓存对照不再需要临时注入臂时，连同对应实验分支一起删除。
