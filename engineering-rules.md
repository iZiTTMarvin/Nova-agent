# Nova Agent 长期工程开发规则

## 1. 目的与适用范围

本文定义 Nova Agent 长期有效的工程约束。它不描述某一版本“现在有哪些文件”，而是规定功能增长、缺陷修复、重构和迁移时必须保持的结构属性：

- 任何行为都能找到唯一负责模块；
- 任何可变状态都能找到唯一写入 Owner；
- 依赖方向可解释、可扫描、不可绕过；
- 跨边界数据有显式且唯一的契约；
- 修改范围、兼容期限和验证证据可审查；
- 代码规模增长时，复杂度仍被领域边界吸收，而不是堆入少数万能模块。

本文适用于所有生产代码、测试、脚本和工程配置，也适用于人类开发者、Coding Agent 和自动化生成代码。`必须`、`禁止` 表示合入前必须满足；`应` 表示默认要求，偏离时必须记录可审查的理由。

现有代码只提供事实证据，不自动构成规则。如果旧实现与本文冲突，应把它登记为债务并阻止继续扩散；不得为了“保持一致”复制错误结构。

---

## 2. 架构判断的最小单位

### 2.1 一句话职责

每个模块、Service、Store、Coordinator、Facade 和公共类型集合都必须能用一句话描述核心职责。

合格的一句话应同时回答：

1. 它拥有什么决策或状态；
2. 它对外提供什么结果；
3. 哪些相邻职责明确不属于它。

例如：

> “运行协调器拥有一次 run 的生命周期状态，并以有序快照对外发布；它不解释具体 Workflow 阶段语义。”

以下描述不合格：

- “处理 Agent 相关逻辑”；
- “提供各种工具函数”；
- “管理会话、模型、权限以及其他功能”；
- “主服务”“核心管理器”“公共帮助方法”。

无法写出一句话职责，通常说明模块混合了领域、生命周期或抽象层级。此时应先澄清 Owner，而不是继续增加方法。

### 2.2 拆分标准不是行数

长文件不必然错误，短文件也不必然清晰。需要拆分的可靠信号是：

- 存在两个或更多彼此独立的变化原因；
- 状态具有不同生命周期、并发规则或持久化策略；
- 高层编排与底层细节混在一起；
- 修改一个行为需要触碰多个不相干区域；
- 测试只能通过构造大量无关依赖来覆盖一个小行为；
- 模块名称无法准确覆盖其公开 API。

不应拆分的情形：

- 拆出的文件只有几行转发，且没有独立概念或契约；
- 拆分只为了满足行数阈值；
- 拆分后依然共享同一批可变字段，只是把方法搬到别处；
- 新文件需要频繁访问原类内部状态，形成“伪模块化”。

---

## 3. 职责边界

### 3.1 单一核心职责

一个模块只能拥有一个清晰的核心职责。它可以包含完成该职责所需的多个步骤，但不能同时拥有不相关的业务决策、状态生命周期和基础设施细节。

判断方法：

- 如果两个部分可独立变化、独立测试、独立替换，应由不同 Owner 管理；
- 如果一个模块只是在调用其他 Owner，可以做编排，但不得复制其规则或状态；
- 如果一个 Facade 暴露多个能力，它只能提供稳定入口，不得成为能力实现和状态真源。

命名为 `Service`、`Manager`、`Coordinator`、`Facade` 或 `Store` 不会自动赋予无限职责。每新增一个方法，都必须能证明它属于该模块的一句话职责。

### 3.2 Agent kernel 的边界

Agent Loop 的唯一核心职责是：

> 驱动“模型输出—工具执行—工具结果回灌—继续或停止”的循环。

允许属于 kernel 的能力：

- 向模型发起一轮请求；
- 识别本轮工具调用；
- 请求工具执行端口执行；
- 将工具结果按协议写回上下文；
- 根据明确的停止策略决定继续、完成、失败或取消；
- 通过端口发布循环事件。

不得由 kernel 拥有的能力：

- 权限规则、用户批准队列和授权持久化；
- 会话、消息、checkpoint、artifact 或记忆的落盘策略；
- 上下文压缩策略与空闲压缩生命周期；
- Provider/协议兼容和 transport 重试细节；
- Skill 发现、加载和调用策略；
- Workflow/XForge 阶段、产品路由和恢复策略；
- Electron 窗口、IPC、Renderer 状态和 UI 展示；
- 产品级 run 生命周期、分支切换或工作区租约。

kernel 可以依赖这些能力的窄接口，但不能知道其具体实现，也不能维护它们的第二份状态。

### 3.3 独立能力必须有独立 Owner

以下能力必须分别有明确 Owner：

| 能力 | Owner 应拥有 | 其他模块只能做 |
| --- | --- | --- |
| 权限 | 授权规则、pending 请求、批准/拒绝语义 | 提交授权请求并消费结果 |
| 持久化 | schema、事务、迁移、恢复和落盘顺序 | 通过 repository/store 接口读写 |
| 上下文压缩 | 阈值、压缩生命周期、压缩簿记和写回资格 | 请求压缩或读取只读结果 |
| 协议适配 | wire 格式、Provider 差异、错误归一化 | 使用统一模型接口 |
| Skill | 发现、优先级、装载和调用契约 | 通过 Skill 端口解析或执行 |
| Workflow | 阶段状态、转移规则、恢复和终态 | 提交命令并读取快照 |
| 运行生命周期 | run 状态、序列、终态提交和交互归属 | 发起动作、订阅投影 |
| UI | 交互状态和领域状态的只读投影 | 不反向成为 runtime 真源 |

若一项能力已有 Owner，新入口必须调用它，不得新建平行 Service 或在调用方复制规则。

### 3.4 编排层与实现层

编排层可以：

- 按固定顺序调用多个 Owner；
- 传递显式上下文和取消信号；
- 将结果组合为更高层 outcome；
- 在 `finally` 中执行它自己拥有的生命周期清理。

编排层禁止：

- 修改被编排模块的内部状态；
- 重新解释被编排模块的错误或终态，使两个地方都能决定最终结果；
- 保存一份“方便查询”的平行权威状态；
- 因调用方便而吸收权限、持久化或兼容逻辑。

---

## 4. 状态所有权

### 4.1 唯一写入 Owner

每项可变状态必须有一个且只有一个写入 Owner。Owner 负责：

- 定义合法状态及转移；
- 校验写入前置条件；
- 处理并发、幂等和取消；
- 持久化与内存状态的一致性；
- 发布不可变快照或事件；
- 在失败和终态时完成清理。

其他模块只能：

- 读取不可变快照；
- 订阅事件；
- 通过 command/request 请求 Owner 修改；
- 保存可丢弃、可重建且明确标注的只读投影。

“多个地方都能 `set`，但大家约定不要冲突”不是所有权。

### 4.2 真源、投影和缓存

必须明确区分：

- **真源**：决定系统事实的唯一状态；
- **投影**：由真源派生、用于另一层消费的只读形状；
- **缓存**：可删除并从真源重建的性能优化；
- **草稿**：尚未提交为事实的临时输入。

投影和缓存不得被反向写回真源。若投影需要接收命令，应将命令转发给 Owner，并等待新快照，不得直接修改本地副本来“保持同步”。

允许乐观 UI，但必须具备：

- 明确的 pending 状态；
- 关联 command/run/generation 的标识；
- 权威结果到达后的对账；
- 失败、过期和乱序事件的回滚或丢弃规则。

### 4.3 禁止重复状态和隐式全局状态

禁止：

- 两个 Store/Service 分别维护同一 `isRunning`、`currentMessageId` 或 lifecycle status；
- 通过多个 setter 手工同步同一事实；
- 可变 module-level Map/Set 没有明确生命周期和清理 Owner；
- 使用“当前实例”“最近一次 run”等隐式全局选择替代显式 ID；
- 用事件顺序碰巧一致来维持状态一致；
- 将可变对象引用跨模块暴露并允许直接修改。

同名字段不一定是重复状态。例如 checkpoint 的 `messageId` 与 run 的 `messageId` 可以分别属于不同生命周期；但必须能说明各自语义、写入者和一致性关系。

### 4.4 并发与终态

涉及异步和并发的状态必须显式定义：

- identity：由 sessionId、runId、messageId、generation 等哪个标识归属；
- authority：谁有资格提交当前变更；
- ordering：sequence、version 或事务顺序；
- idempotency：重复命令和重复事件的结果；
- terminal：谁能提交终态，是否 exactly-once；
- cleanup：失败、取消、超时和宿主销毁时清理什么；
- stale fencing：过期异步结果如何被拒绝。

不得以共享布尔值、延时、无限重试或“最后写入获胜”代替这些协议。

---

## 5. 依赖方向

### 5.1 顶层依赖规则

Nova 的顶层依赖方向如下：

```text
renderer ──┐
preload  ──┼──> shared
main ──────┼──> runtime ──> shared
           └──> shared
```

| 层 | 允许依赖 | 禁止依赖 |
| --- | --- | --- |
| `shared` | 自身、环境中立的纯类型/纯函数 | runtime、main、preload、renderer、Electron 和基础设施实现 |
| `runtime` | shared、同层公开契约、标准库或经边界隔离的领域适配器 | main、preload、renderer、Electron UI |
| `main` | runtime、shared、Electron、主进程基础设施 | renderer、preload 内部实现 |
| `preload` | shared、Electron 的桥接 API | runtime、main、renderer 内部实现 |
| `renderer` | shared、renderer 内部、浏览器/React 生态 | runtime、main、preload 内部实现、Electron 主进程 API |
| `test-support` | 被测层的公共契约 | 被生产代码依赖 |

`runtime` 内部仍需保持依赖倒置：Agent kernel 和领域决策依赖端口，不依赖数据库、文件格式、网络 transport 或 Electron。具体适配器可以位于 runtime 的领域目录，但不得反向渗入 kernel。

### 5.2 同层也有方向

同处一个顶层目录不代表可以任意互相 import。至少遵循：

- core/domain 不依赖产品路由和 feature executor；
- 通用 run 生命周期不依赖具体 Workflow/XForge 语义；
- Service/host 不依赖 IPC handler；
- 协议适配器依赖领域端口，领域逻辑不依赖适配器；
- 一个领域只通过公开入口访问另一个领域。

如果 A 与 B 互相需要，应提取双方都真正依赖的稳定契约，而不是建立循环 import。提取契约时不得顺带创建包含所有类型的 `common`。

### 5.3 公开入口与内部实现

领域目录应通过明确的公共入口导出稳定 API。调用方禁止：

- 深路径访问另一个领域的内部文件；
- 从 barrel 重导出不属于本领域 ownership 的实现；
- 用相对路径穿越目录以规避 alias 或架构测试；
- 复制内部类型来避免依赖公共契约；
- 用动态 import 或 `require` 绕过静态边界。

公开入口不是“把目录内所有内容全部 export”。只导出调用方需要且承诺稳定的契约。

### 5.4 边界债务

确实无法一次消除的依赖债务必须使用精确 allowlist，并满足：

- 精确到 from、to 和规则，不允许目录通配；
- 说明为何存在、目标 Owner 和删除条件；
- 新债务需经过明确审查，不能因测试失败随手加入；
- 债务消失后 allowlist 必须因 stale 而失败，迫使删除条目。

循环依赖不应进入 allowlist。

---

## 6. 目录与文件组织

### 6.1 领域优先

目录首先表达业务领域或稳定技术职责，其次才表达文件类型。一个功能的实现、契约、适配器和测试必须可定位：

```text
domain/
  index.ts
  contracts.ts
  service.ts
  adapters/
tests/
  .../domain/
```

这只是形状示例，不要求机械创建所有文件。没有独立概念时，应保持合并。

### 6.2 禁止垃圾桶目录

不得新增通用 `utils`、`helpers`、`common` 目录或文件来存放“暂时不知道放哪里”的代码。

可接受的共享抽象必须：

- 有可命名的单一职责，如 `pathSafety`、`messageProjection`；
- 有明确消费者和稳定契约；
- 不依赖调用方的隐式状态；
- 不成为跨层访问内部实现的捷径。

当一个函数只服务一个领域时，留在该领域。只有概念真的跨领域稳定复用时才提升层级。

### 6.3 工具组织

每个工具是独立能力单元，应放在自己的领域子目录，并通过该目录公共入口导出。工具 schema、executor、类型和专属测试应容易一起找到。

禁止继续在工具根目录平铺新工具。现有平铺文件属于结构债务：修改相关工具时应在不扩大任务范围的前提下逐步归位，但不得为“整理目录”单独制造大规模无行为收益的搬迁。

### 6.4 文件拆分与合并

拆分时应沿以下边界之一进行：

- 独立状态 Owner；
- 独立领域概念；
- 独立协议或公共契约；
- 独立生命周期；
- 可替换适配器；
- 可独立验证的纯策略。

若拆分后两个文件仍需共同修改、共享内部可变状态且不能单独测试，应重新评估。反过来，多个只有转发作用且没有独立语义的小文件应合并到真正 Owner 中。

---

## 7. 契约与类型

### 7.1 跨模块通信

跨模块、跨进程或跨持久化边界必须使用显式、类型化契约，包括：

- command/request 参数与结果；
- event 名称、payload 和顺序语义；
- 状态快照与版本；
- tool 输入输出；
- 持久化 schema 和迁移结果；
- 错误码、失败类型和取消语义。

公共命令、事件和数据结构必须有唯一来源。消费者应 import 该来源或其窄 projection，不得复制“差不多一样”的 interface。

### 7.2 禁止松散类型

禁止使用：

- `any`、`any[]`、`Record<string, any>`；
- 无 discriminant 的宽联合；
- 无 schema 的 `Record<string, unknown>` 作为长期公共 payload；
- 仅为压过编译器的双重断言；
- 复制类型后各自演化；
- 用可选字段堆出多个互斥状态。

外部输入应先作为 `unknown`，在边界完成运行时校验和归一化，再交给领域层。确需开放扩展字段时，也必须把稳定字段、扩展字段和验证责任写入契约。

### 7.3 分离不同语义的数据

以下内容不得混在同一个无结构对象中：

- transport 元数据与领域状态；
- 可持久化事实与仅运行期句柄；
- 用户可见结果与调试诊断；
- 命令意图与执行回执；
- 权威状态与 UI 展示派生字段。

应使用明确的 DTO、domain state、receipt、diagnostic record 或 projection，并在边界做转换。转换只能有一个权威实现。

### 7.4 版本与兼容

持久化和跨进程契约变更必须：

- 显式标记 schema/version 或可判定的格式；
- 在单一迁移入口归一化旧数据；
- 迁移后让领域逻辑只处理当前形状；
- 对未知版本 fail closed 或给出明确错误；
- 用 fixture/contract test 覆盖旧版本读取和新版本写入。

不得把 `typeof field`、缺字段 fallback 等兼容判断散落在业务路径各处。

---

## 8. 修改原则

### 8.1 变更说明

每次修改前必须形成最小变更说明：

```text
目标：本次唯一需要改变的行为或结构。
范围：允许修改的 Owner、调用链和契约。
非目标：明确不处理的相邻问题。
不变量：必须保持的行为、数据和安全边界。
验收：测试、类型、架构、构建或人工验证证据。
```

对于小改动，可以是 PR/任务中的五行说明；对于跨模块改动，应补充数据流、失败模式、迁移和回退。

### 8.2 最小且完整

“最小”指不扩大目标，“完整”指在正确 Owner 处修复根因。两者缺一不可。

禁止：

- 在多个下游位置重复增加判断；
- 为局部需求复制一套流程；
- 新增第二个状态源后靠同步维持；
- 静默吞错、无依据 fallback、魔法延时或无限重试；
- 借修复顺便重命名、搬目录、统一风格或重写稳定模块；
- 保留失去用途的分支、导出、配置和注释。

### 8.3 新旧路径迁移

不允许新旧两套主路径长期并存。迁移必须定义：

1. 当前唯一真源；
2. 新路径切入点；
3. 是否存在短期 dual-read/dual-write；
4. 对账与失败行为；
5. 删除旧路径的客观条件；
6. 保护兼容期的测试；
7. 条件满足后由谁删除。

若无法说明删除条件，新兼容层不得合入。

### 8.4 兼容代码

兼容代码的注释或邻近文档必须说明：

- 兼容的外部版本、持久化格式或公开调用方；
- 删除会破坏什么；
- 删除条件如何被验证；
- 正常主路径为何不会依赖该分支。

“历史原因”“暂时保留”“保险起见”不是有效理由。内部未发布实现通常不应通过长期兼容层保留，而应完成一次性迁移并删除旧路径。

### 8.5 高风险重构

当无法证明行为保持一致时，不得直接实施高风险重构。应先：

- 建立 characterization tests；
- 记录事件顺序、状态快照和持久化 fixture；
- 将变更拆成可独立回退的阶段；
- 每阶段保持绿色基线并实际删除被替代实现；
- 对 clean checkout 执行门禁。

只新增抽象而不删除旧职责，不算完成重构。

---

## 9. 测试与验证

### 9.1 测试应保护什么

测试不仅验证最终结果，还应按风险保护：

- 事件顺序、去重和乱序处理；
- 状态只能由 Owner 修改；
- 终态 exactly-once；
- 失败、取消、超时后的资源清理；
- generation/version fencing；
- 权限 fail-closed；
- 协议序列化、反序列化和迁移；
- import 边界和无循环依赖；
- 正常路径在错误修复后保持不变。

### 9.2 禁止假绿测试

以下测试不能单独证明行为正确：

- 只断言返回值非空；
- 只断言 mock 被调用；
- 只验证 happy path；
- mock 掉真正的状态 Owner、事务或文件系统失败点；
- 对错误使用宽泛 snapshot，无法指出错误语义；
- 为让测试通过而删除、跳过或放宽已有断言。

Mock 用于隔离边界，不用于替代被验证的行为。关键状态清理、持久化事务和协议转换应尽量使用真实实现或最小可控 fake。

### 9.3 影响范围对应的门禁

| 变更类型 | 最低验证 |
| --- | --- |
| 纯函数/领域规则 | 定向单测、类型检查 |
| 状态或生命周期 | 定向单测、失败/取消/并发测试、类型检查 |
| 跨模块契约 | producer/consumer contract test、类型检查、架构边界测试 |
| 持久化/schema | migration fixture、读写往返、损坏输入、类型检查 |
| Renderer 交互 | 状态测试、关键交互/E2E 或可复现人工验证、类型检查 |
| 构建/资源/依赖 | 相关验证脚本、生产 build |
| 纯文档 | 链接/路径、格式、diff 和 tracked/ignored 状态检查 |

所有源码变更还必须执行仓库已配置的 lint。若项目尚未配置所需门禁，应将其记录为工程缺口，不能把“命令不存在”描述为通过。

全量测试不是定向测试的替代品；定向测试也不是 typecheck、架构测试或 build 的替代品。验证强度应与风险匹配。

### 9.4 架构测试

能机械验证的规则不应只依赖文档。至少应持续维护：

- 顶层和同层 import 边界扫描；
- 精确、双向对账的边界债务 allowlist；
- 循环依赖检测；
- 公共契约编译/运行时校验；
- 禁止 `any` 和禁止生产代码依赖 test-support 的 lint/扫描；
- 关键状态机转移和终态协议测试。

规则文档定义意图，自动化门禁阻止退化，两者缺一不可。

---

## 10. AI 开发纪律

### 10.1 实现前必须读取

Coding Agent 在修改前必须读取：

1. 根 `AGENTS.md` 和适用的目录级规则；
2. 目标模块的一句话职责或公共入口；
3. 真实调用方与被调用方；
4. 公共类型、事件和持久化契约；
5. 相邻实现和现有测试；
6. 当前 diff、tracked/untracked 文件和相关生成流程。

不得只根据报错文本或最近文件猜测根因。

### 10.2 根因优先

AI 必须定位最早破坏不变量的位置。若症状出现在 UI，但真源或事件路由更早出错，应在权威 Owner 修复；若底层状态正确，则不得无依据重写底层稳定模块。

禁止：

- 在最近出现错误的位置先加特殊判断；
- 为了缩短文件机械拆分；
- 为了“更现代”替换稳定架构；
- 未经授权扩大范围、添加依赖或改变公共行为；
- 把注释、测试变绿或文件变短当作完成证据。

### 10.3 强制停止并汇报

遇到以下情况，AI 应停止实现并报告事实、风险和所需决策：

- 职责归属不清，无法写出一句话 Owner；
- 同一状态存在多个写入者；
- 同一公共事件/数据有冲突类型；
- 正确实现需要违反依赖方向；
- 只能靠长期双主路径、重复状态或模糊兼容完成；
- 需求会改变公开 API、数据格式、安全边界或不可逆操作，但未获授权；
- 无法建立足以证明高风险重构行为保持一致的测试。

停止不是放弃。报告应指出已检查证据、冲突位置、可选收敛方向和最小决策点。

### 10.4 交付报告

AI 只报告真实完成和执行过的验证。最终至少说明：

- 完成的目标；
- 根因或关键设计判断；
- 修改的 Owner 与契约；
- 被明确保护的旧行为；
- 实际运行的命令和结果；
- 未验证项、剩余风险、兼容删除条件；
- 工作区中不属于本任务的改动。

---

## 11. Nova 架构中的示例

以下仅用于说明规则，不是永久文件清单。组件重命名或搬迁后，规则仍成立。

### 11.1 正确的 Owner 分离

- 通用 run 协调器作为生命周期、sequence 和终态的真源；Workflow 只提交 feature 状态与投影，Renderer 只消费快照。
- 权限协调器拥有 pending resolver 与授权结果；Agent kernel 只调用 `authorize` 端口。
- 压缩服务拥有阈值、active/idle 压缩和写回资格；Agent kernel 只在明确时机请求压缩。
- 产品分派器把已解析 route 交给 Skill/Workflow/XForge executor，并返回声明式 outcome；它不拥有 run、checkpoint 或 UI 状态。
- IPC handler 只做 transport 适配和输入校验；生命周期编排属于主进程应用服务。

### 11.2 可接受的投影

Renderer 可以把权威 run snapshot 投影成按钮的 `isBusy`，但该值必须可从 snapshot 重建。若另一个 Store 也可直接写 `isBusy`，再靠事件手工同步，就形成了第二状态源，应收敛为 selector 或单向投影。

### 11.3 文件长度不是结论

一个较长的状态机若只拥有 run 转移并有完整协议测试，可以保持集中。一个较短的 Facade 若同时决定权限、持久化、路由和 UI 事件，仍然违反职责边界。

### 11.4 兼容路径

读取旧会话格式时，应由迁移入口把旧 schema 转为当前 domain model。业务循环不应在每次读取消息时继续判断多个历史字段。兼容期结束后，按可验证条件删除旧 reader 和 fixture。

---

## 12. 执行与治理

### 12.1 文档入口

- 根 `AGENTS.md` 保留短、强制、可频繁读取的规则和检查清单。
- 本文保存完整理由、例子和审查标准。
- 目录级 `AGENTS.md` 只能增加本领域约束，不能削弱根规则。
- 规则变更必须与架构演进一起评审，不能因单次任务失败临时放宽。

### 12.2 自动化执行

长期应由 CI 和本地门禁共同执行：

1. typecheck；
2. lint，包括 `any`、未处理 Promise 和危险断言；
3. import boundary 与循环依赖；
4. 定向和全量测试；
5. production build；
6. 文档链接和规则文件可追踪性；
7. clean checkout 验证高风险改动。

任何 allowlist、skip 或 lint disable 都必须精确、邻近、带原因和删除条件。全局关闭规则不接受。

### 12.3 审查问题

每个 PR 或 Agent 交付至少回答：

1. 本次唯一目标是什么？
2. 哪个模块是行为和状态 Owner？
3. 是否新增或改变公共契约？
4. 依赖方向是否仍合法？
5. 是否产生第二状态源、新旧双主路径或兼容债？
6. 失败、取消、并发和恢复如何工作？
7. 哪些测试证明新行为和旧不变量？
8. 哪些代码因替代而被删除？
9. 是否存在未验证项或需要后续删除的兼容层？

回答不清楚时，不应合入。

### 12.4 债务优先级

架构债务按以下顺序处理：

1. **立即处理**：多写入 Owner、安全/权限 fail-open、终态冲突、数据损坏、跨层反向依赖、公共契约冲突；
2. **修改前处理**：目标区域存在双主路径、无边界的万能类、无法测试的隐式状态；
3. **防止恶化**：目录平铺、历史命名、局部文件过长、尚未被修改的兼容 reader；
4. **观察**：只有规模信号但尚无修改扩散、状态冲突或依赖违规证据。

不得因为文件大或代码旧就直接判为紧急；也不得因为测试全绿就忽略状态 Owner 和依赖方向问题。
