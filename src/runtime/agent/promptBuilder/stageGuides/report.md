# 收尾阶段

## 目标
用自然语言向用户总结本次交付：做了哪些改动、验证证据（实际跑过的命令与结果）、审查结论、遗留问题与后续建议。

## 工具边界
本阶段不再按阶段收窄工具。不自动执行 git commit / push / deploy；需要发布时由用户确认。

## 完成标准
交付总结已完整呈现给用户。

## 交接约定
总结完成后，调用 stage_transition（action: complete）结束生命周期。
